--- gnumeric-1.0.9/plugins/psiconv/Makefile.am~	Fri Sep 20 06:27:11 2002
+++ gnumeric-1.0.9/plugins/psiconv/Makefile.am	Fri Sep 20 06:27:11 2002
@@ -8,7 +8,7 @@
 gnumeric_plugin_psiconvdir = $(gnumeric_plugindir)/psiconv
 xmldir = $(gnumeric_plugin_psiconvdir)
 gnumeric_plugin_psiconv_LTLIBRARIES = psiconv.la
-psiconv_la_LDFLAGS = -module -avoid-version $(PSICONV_LIBS)
+psiconv_la_LDFLAGS = -module -avoid-version
 psiconv_la_SOURCES = boot.c psiconv-read.c psiconv-plugin.h
 
 xml_in_files = plugin.xml.in
--- gnumeric-1.0.9/configure.in~	Fri Sep 20 22:23:01 2002
+++ gnumeric-1.0.9/configure.in	Fri Sep 20 22:23:01 2002
@@ -116,29 +116,6 @@
 
 
 dnl **************************************************
-dnl * psiconv support
-dnl **************************************************
-PSICONV_INIT()
-if test "$want_psiconv" = "yes"; then
-	ac_save_CFLAGS="$CFLAGS"
-	ac_save_LIBS="$LIBS"
-	CFLAGS="$CFLAGS $psiconv_cflags"
-	LIBS="$LIBS $psiconv_libs"
-	AC_CHECK_LIB(psiconv,psiconv_parse_sheet_file,want_psiconv=yes,want_psiconv=no)
-	CFLAGS="$ac_save_CFLAGS"
-	LIBS="$ac_save_LIBS"
-
-	if test "$want_psiconv" = "no"; then
-		PSICONV_LIBS=""
-		PSICONV_CFLAGS=""
-		with_psiconv=false
-	fi
-
-	AM_CONDITIONAL(WITH_PSICONV,$with_psiconv)
-fi
-
-
-dnl **************************************************
 dnl * internationalization support
 dnl **************************************************
 dnl 
--- gnumeric-1.0.9/plugins/Makefile.am~	Fri Sep 20 22:31:17 2002
+++ gnumeric-1.0.9/plugins/Makefile.am	Fri Sep 20 22:31:17 2002
@@ -36,14 +36,8 @@
 GDA_DIR=
 endif
 
-if WITH_PSICONV
-PSICONV_DIR=psiconv
-else
-PSICONV_DIR=
-endif
-
 SUBDIRS_FILE_FORMATS = excel lotus-123 oleo sc sylk xbase html dif \
-	xml_sax applix mps $(GNOME_GLOSSARY_DIR) $(PSICONV_DIR)
+	xml_sax applix mps $(GNOME_GLOSSARY_DIR)
 
 SUBDIRS_FUNCTIONS = $(PYTHON_DIR) $(PERL_DIR) $(GUILE_DIR) $(GB_DIR) \
 	numtheory derivatives $(GDA_DIR) $(PYFUNC_DIR)
